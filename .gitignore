/target
# Cargo.lock IS committed (binary crate → reproducible builds + cargo audit)
.env
.env.local
*.log
.DS_Store
.idea/
.vscode/
/dist
/data
# Local session scratch + Claude tooling config — not for version control
HANDOFF.md
.claude/
